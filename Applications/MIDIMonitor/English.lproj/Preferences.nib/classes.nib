{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                changeAutoSelectFirstSourceIfSourceDisappears = id; 
                changeAutoSelectFirstSourceInNewDocument = id; 
                changeControllerFormat = id; 
                changeDataFormat = id; 
                changeNoteFormat = id; 
                changeOpenWindowsForNewSources = id; 
                changeTimeFormat = id; 
            }; 
            CLASS = SMMPreferencesWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                autoSelectFirstSourceIfSourceDisappearsCheckbox = NSButton; 
                autoSelectFirstSourceInNewDocumentMatrix = NSMatrix; 
                controllerFormatMatrix = NSMatrix; 
                dataFormatMatrix = NSMatrix; 
                noteFormatMatrix = NSMatrix; 
                openWindowsForNewSourcesCheckbox = NSButton; 
                timeFormatMatrix = NSMatrix; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}